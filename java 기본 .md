🚦 디버깅할 때 코드 실행 순서(= 실제 JVM 실행 순서)
🔥 핵심 규칙 딱 3개

main 메서드부터 시작한다

위에서 아래로 한 줄씩 실행한다

메서드를 만나면 → 그 메서드 안으로 들어가서 실행한다

이걸 “스택(Stack) 프레임 구조”로 진행해.

```📌 예시 코드로 흐름을 눈으로 따라가보자
public class Main {
    public static void main(String[] args) {
        int a = 10;              // (1)
        int b = add(a, 5);       // (2) 메서드 호출 → add로 점프
        System.out.println(b);   // (5)
    }

    public static int add(int x, int y) {
        int result = x + y;      // (3)
        return result;           // (4) 메인으로 돌아감
    }
}
```
🧭 디버깅 실제 흐름
1) (main 시작) JVM이 main()을 호출

➡️ 스택에 Main 프레임이 딱 생김

2) int a = 10 실행

디버깅 커서(노란 화살표)가 여기 있으면
👉 이 줄이 실행됨

a 변수(10)가 스택에 저장됨

3) int b = add(a, 5); 도착

여기서 중요한 포인트:

메서드를 만나면 → JVM은 그 메서드로 이동한다(점프!)

디버깅에서는 보통:

Step Into(F5) → add() 안으로 들어감

Step Over(F6) → add() 실행만 하고 결과만 받음

4) add() 안으로 들어감

add 메서드 프레임이 스택에 새로 생김

main()
  └ add()

5) add 내부 실행
int result = x + y;   // (3)
return result;         // (4)


여기까지 실행되면 add()는 끝남 → 스택에서 제거됨
그리고 결과값(result)이 main으로 다시 전달됨

6) main으로 돌아와서 실행 재개

add가 끝나면 다시 main의 다음 줄로 이동함:

System.out.println(b);

7) 프로그램 종료

main도 끝나면 → 스택 프레임이 비게 되고 프로그램 종료

✔️ 요약 흐름표
실행순서	동작
1	JVM → main() 호출
2	main 안에서 한 줄씩 실행
3	메서드 호출 만나면 → 그 메서드로 이동
4	메서드 끝나면 → 원래 위치로 복귀
5	main 끝나면 프로그램 종료
🧠 디버깅 시 “이해해야 할 핵심 4개”
✔ 1. 메서드는 “들어갔다 → 나왔다”로 보면 된다

→ 진짜 함수 안으로 들어갔다가, return 때 다시 돌아옴

✔ 2. 각 메서드는 “스택에 따로 공간”을 만든다

메서드가 호출될 때마다
새로운 스택 프레임이 생김

메서드 끝나면 그 프레임 사라짐

✔ 3. 참조변수는 “주소”를 들고 이동한다

디버깅 중에 객체를 보면 → 주소값이 유지되는 거임

✔ 4. 디버깅은 “한 줄씩 읽기”가 아니라

👉 “스택이 어떻게 열리고 닫히는지”를 보는 것이다.

🌟 완전 쉬운 비유

메서드 호출은 “문을 열고 방에 들어갔다가, 일이 끝나면 다시 나오는 것”

main 방에서 add 방으로 들어감

할 일 하고 return 하면 → 다시 main 방으로 돌아옴



🎯 멤버변수(Member Variable)란?
👉 클래스 내부에 선언된 변수
👉 객체가 가지는 데이터(속성)
👉 힙(Heap)에 저장되는 변수

즉,

“객체가 가지고 있는 변수 = 멤버변수”

🧩 예시로 바로 이해하기
class Person {
    String name;    // ← 멤버변수
    int age;        // ← 멤버변수

    void hello() {
        String msg = "안녕"; // ← 지역변수
    }
}


name, age → 멤버변수

msg → 지역변수

📦 멤버변수는 어디에 저장될까?

✔ 힙(Heap)
객체를 new 로 생성할 때
멤버변수들은 전부 힙에 저장된다.

Person p = new Person();


메모리 구조는 이렇게 됨:

스택(stack)
 └─ p (참조변수) → 주소 저장

힙(heap)
 └─ Person 객체
        ├ name
        └ age

🔥 멤버변수의 특징 정리
✔ 1. 클래스 전체에서 사용 가능 (클래스 스코프)

블록이 끝나도 사라지지 않음
→ 객체가 살아있는 동안 계속 존재

✔ 2. 객체당 하나씩 존재

new 할 때마다 새로운 멤버변수 세트가 생김

예:

Person p1 = new Person(); // p1.name, p1.age
Person p2 = new Person(); // p2.name, p2.age


서로 독립적!

✔ 3. 힙에 저장됨

지역변수는 스택
멤버변수는 힙
→ 스코프와 생명주기가 완전 다름

✔ 4. default 값을 가진다

지역변수는 초기화 안 하면 오류
BUT
멤버변수는 자동 초기화됨:

타입	기본값
int	0
double	0.0
boolean	false
참조형	null
💡 멤버변수와 this의 관계
class Person {
    String name; // 멤버변수

    Person(String name) {
        this.name = name; 
        // this.name → 멤버변수
        // name → 파러미터
    }
}


이때 this.name이라고 쓰는 이유:

이름이 겹치면 멤버변수를 가리키는 표시가 필요함 → 그게 this

🧠 멤버변수 vs 지역변수 비교표
비교	멤버변수	지역변수
선언 위치	클래스 내부	메서드/블록 내부
저장 위치	힙(Heap)	스택(Stack)
기본값	자동 초기화 O	자동 초기화 X
스코프	객체가 살아있는 동안	블록 끝나면 사라짐
접근	this로 접근 가능	this로 접근 불가
🌟 한 줄 정리

멤버변수 = 클래스 안에 선언된, 객체가 가지고 있는 “속성”이며 힙에 저장된다.







🎯 디펜던시(Dependency) = 종속 관계란?
👉 A가 혼자 못 움직이고, B를 의존해야만 동작하는 관계

즉,

A가 B 없으면 절대 동작 안 함 = A가 B에게 종속된 상태

🧠 완전 쉬운 현실 비유

커피 머신(A)은 **물(B)**이 없으면 커피 못 만든다
→ 커피 머신(A)은 물(B)에 종속되어 있다.

자동차(A)는 엔진(B) 없으면 못 움직인다
→ 자동차(A)는 엔진(B)에 종속되어 있다.

"없으면 못 한다" → 종속관계
"있어야 한다" → 의존성(dependency)

🧩 코드로 바로 이해하기 (초보자 버전)
✔ 종속관계(Dependency) 예시
class Engine {
    void start() {
        System.out.println("엔진 켜짐");
    }
}

class Car {
    Engine engine = new Engine();  // 🔥 Car가 Engine에 종속됨

    void drive() {
        engine.start();
    }
}

✔ 분석

Car는 Engine 없으면 drive() 자체가 안 됨

그래서 Car → Engine 으로 종속관계

즉, Car가 Engine에 디펜던시(Dependency) 있다!

🔥 한 줄 요약

디펜던시 = A가 B 없으면 동작 못하는 관계
종속관계 = A → B 의존

완전히 같은 뜻.

📌 Dependency가 왜 중요할까?

이걸 알아야:

타이트 커플링 / 루즈 커플링 구분됨

엔진 교체가 왜 어려운지 이해됨

Interface를 왜 쓰는지 이해됨

Spring DI(의존성 주입) 구조 이해됨

유지보수 왜 어려운지 이해됨

🚨 타이트 커플링도 사실 "강한 디펜던시"이다

예:

Engine engine = new Engine();


이렇게 클래스 안에서 new 하면 → 강한 종속관계
= 타이트 커플링

🧘 루즈 커플링은 “약한 디펜던시”

예:

class Car {
    Engine engine; // 선언만 (약한 종속)

    Car(Engine engine) {  
        this.engine = engine;  // 외부에서 넣어줌 (DI)
    }
}


Car는 이제 어느 엔진이 와도 됨
→ 약한 종속관계
→ 루즈 커플링

⭐ 서령 암기 버전 (이거만 외워!)
🔥 디펜던시 = A가 B가 있어야 한다
🔥 종속관계 = A → B 필요
🔥 new 하면 강한 종속 = 타이트 커플링
🔥 외부에서 넣어주면 약한 종속 = 루즈 커플링(DI)



1️⃣ 비정적 내부 클래스(Inner class) 예제

Outer 객체가 먼저 있어야 Inner를 만들 수 있음

Inner는 Outer의 필드/메서드에 그냥 바로 접근 가능

// 파일 이름: OuterExample.java
public class OuterExample {

    private String name = "Outer Name"; // 외부(Outer) 클래스의 인스턴스 필드

    // 비정적 내부 클래스 (inner class)
    class Inner {
        void printInfo() {
            // 외부 클래스의 필드와 메서드에 직접 접근 가능
            System.out.println("Inner에서 name 접근: " + name);
            sayHello();
        }
    }

    void sayHello() {
        System.out.println("Outer의 sayHello() 호출");
    }

    public static void main(String[] args) {
        // 1) 먼저 외부 클래스 객체 생성
        OuterExample outer = new OuterExample();

        // 2) 외부 객체로부터 내부 클래스 객체 생성
        OuterExample.Inner inner = outer.new Inner();

        // 3) 내부 클래스 메서드 호출
        inner.printInfo();
    }
}

흐름 정리

OuterExample = 최상위(바깥) 클래스

class Inner = 비정적 내부 클래스

OuterExample outer = new OuterExample();

OuterExample.Inner inner = outer.new Inner(); ← 이 패턴 중요

inner.printInfo(); 안에서

name 바로 사용

sayHello() 바로 호출

→ “내부 클래스는 외부 클래스 객체에 붙어서 돌아간다” 가 코드로 보이는 예제.

2️⃣ static 중첩 클래스(Static nested class) 예제

외부 객체 필요 없이 클래스처럼 독립적으로 사용

하지만 파일을 따로 안 만들고, 바깥 클래스 안에 “묶어서” 넣는 용도

// 파일 이름: AccountExample.java
public class AccountExample {

    private String owner;   // 인스턴스 필드
    private int balance;    // 인스턴스 필드

    public AccountExample(String owner, int balance) {
        this.owner = owner;
        this.balance = balance;
    }

    // 정적 중첩 클래스 (static nested class)
    public static class Validator {
        // 계좌가 유효한지 검사하는 헬퍼 메서드
        public static boolean isValidBalance(int balance) {
            return balance >= 0;
        }
    }

    public void deposit(int amount) {
        if (!Validator.isValidBalance(amount)) {
            System.out.println("잘못된 금액입니다.");
            return;
        }
        balance += amount;
    }

    public void printInfo() {
        System.out.println("소유자: " + owner + ", 잔액: " + balance);
    }

    public static void main(String[] args) {
        AccountExample acc = new AccountExample("서령", 1000);
        acc.printInfo();

        acc.deposit(500);
        acc.printInfo();

        acc.deposit(-100); // 잘못된 금액 처리 예시
    }
}

흐름 정리

AccountExample = 최상위 클래스

public static class Validator = static 중첩 클래스

사용은 그냥:

AccountExample.Validator.isValidBalance(100);


이때 외부 객체 필요 없음 (new AccountExample 안 해도 됨)

→ “헬퍼(도우미) 클래스인데, 이 파일(AccountExample) 안에서만 쓰고 싶다”
→ 그래서 static 중첩 클래스로 안에 넣어두는 것
→ 패키지에 파일(클래스) 안 늘어나서 깔끔.

3️⃣ 한 번에 정리

비정적 내부 클래스 (class Inner)

문법: outer.new Inner()

외부 인스턴스(객체)에 종속

외부 필드/메서드에 바로 접근 가능

static 멤버 정의 불가

static 중첩 클래스 (static class Nested)

문법: Outer.Nested nested = new Outer.Nested();

외부 인스턴스 필요 없음

사실상 “독립 클래스”처럼 동작하지만, 파일 안에서만 묶어서 쓰려고 넣는 것

static 필드/메서드 정의 가능


